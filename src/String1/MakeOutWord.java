package String1;

//Учитывая «out» длину строки 4, например «<< >>», и слово,
// верните новую строку, в которой слово находится в середине строки,
// например «<< слово >>». Примечание: используйте str.substring (i, j) для извлечения строки,
// начинающейся с индекса i, и перехода вверх, но не включая индекс j.
 /*makeOutWord («<< >>», «Yay») → «<< Yay >>»
makeOutWord («<< >>», «WooHoo») → «<< WooHoo >>»
        makeOutWord ("[[]]" , "word") → "[[word]]"*/

public class MakeOutWord {
    public String makeOutWord(String out, String word) {
    return "";
    }
}
